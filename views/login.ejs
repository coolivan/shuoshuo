<% include header.ejs %>


<div class="container main">
    <!-- Example row of columns -->
    <div class="row">
        <form>
            <div class="form-group">
                <label for="exampleInputEmail1">User name</label>
                <input type="email" class="form-control" id="username" name="username" placeholder="用户名，可以是中文，不能和别人重复">
            </div>
            <div class="form-group">
                <label for="exampleInputPassword1">Password</label>
                <input type="password" class="form-control" id="password" name="password" placeholder="密码，至少6位">
            </div>

            <button type="button" class="login btn btn-default">登录</button>
        </form>
    </div>


</div> <!-- /container -->



<script>

    $(function () {
        $(".login").click(function(){
            $.post("/dologin",{
                "username":$("#username").val(),
                "password":$("#password").val()
            },function (result) {
                if(result == "-4"){
                    alert("系统错误");
                }else if (result == "-2"){
                    alert("用户不存在");
                }else if(result == "-1"){
                    alert("密码错误")
                }else {
                    // alert("登录成功");
                    window.location.href="/";
                }
            });

        })
    })
</script>

<% include footer.ejs %>